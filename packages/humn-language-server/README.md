# Humn Language Server

Language Server Protocol implementation for Humn single-file components.

It provides editor intelligence for `.humn` files, including diagnostics, completions, go-to-definition, hover, references, rename, document symbols and semantic tokens.

The VS Code extension consumes this package through `vscode-languageclient`.
